package com.flixclusive.provider.capability.api

import com.flixclusive.model.provider.link.Stream

/** Capability contract for providers that resolve stream links. */
typealias StreamProviderApi = MediaLinkProviderApi<Stream>
